import { test, expect } from '@playwright/test';
import { Given, When, Then } from '@cucumber/cucumber';

// Given('I navigate to the demoqa homepage', async function () {
//   await navigateTo();
// });

// When('I click on the {string} link', async (linkText) => {
//   await homePage.clickOnElement(linkText);
// });

// Then('I should see the {string} heading', async (headingText) => {
//   const isVisible = await homePage.getElementText(headingText);
//   expect(isVisible).toBeTruthy();
// });